<tr class="row">
    <td class="col d-flex justify-content-start align-items-center pl-3">
        <a href="{{ path('single_group', {groupId: group.id}) }}" class="text-reset text-decoration-none stretched-link">{{ group.name }}</a>
    </td>
    <td class="col-2 d-flex justify-content-center align-items-center">
        {{ group.user.count }} members
    </td>
    <td class="col-1 d-flex justify-content-center align-items-center">
        <a href="{{ path('group_delete', {groupId: group.id}) }}" onclick="return confirm('Delete group {{ group.name }}?')" class="btn btn-outline-danger"><i class="fas fa-trash-alt"></i></a>
    </td>
</tr>